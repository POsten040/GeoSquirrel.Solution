@{
  Layout = "_Layout";
}

<h1>Find Games/Index Page</h1>

@foreach(Game game in Model)
{
  @Html.ActionLink($"{game.Name}", "Details", null, new { id = game.GameId});
}






@* @{
    ViewData["Title"] = "Home Page";
}

<h1>All Businesses:</h1>

<table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Industry</th>
                <th>Hours</th>
                <th>Address</th>
                <th>Website</th>
                <th>Phone</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (Business business in Model)
            {
                <tr>
                    <td>@business.BusinessId</td>
                    <td>@business.Name</td>
                    <td>@business.Industry</td>
                    <td>@business.Hours</td>
                    <td>@business.Address</td>
                    <td><a href="@business.URL"><p>@business.URL</p></a></td>
                    <td><a href="tel:@business.Phone"><p>@business.Phone</p></a></td>
                    <td><a class="btn btn-success" href="/Businesses/Details/@(business.BusinessId)"><p>View Details</p></a></td>
                </tr>
            }
        </tbody>
    </table> *@
